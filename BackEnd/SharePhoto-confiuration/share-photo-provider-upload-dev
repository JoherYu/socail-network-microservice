spring:
  application:
    name: share-photo-provider-upload
  zipkin:
    base-url: http://localhost:7020
  boot:
    admin:
      client:
        url: http://localhost:7010
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/social_network?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
      username: root
      password: 123456
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      # MySQL 8.x: com.mysql.cj.jdbc.Driver
      driver-class-name: com.mysql.jdbc.Driver

mybatis:
    type-aliases-package: com.sharePhoto.entity
    mapper-locations: classpath:mapper/*.xml

server:
  port: 7050

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7000/eureka/

fastdfs.base.url: http://192.168.21.87:8888/
storage:
  type: fastdfs
  fastdfs:
    tracker_server: 192.168.21.87:22122