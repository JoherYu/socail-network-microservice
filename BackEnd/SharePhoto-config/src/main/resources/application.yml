spring:
  application:
    name: share-photo-config
  boot:
    admin:
      client:
        url: http://localhost:7010
  cloud:
    config:
      label: master
      server:
        git:
          uri: http://192.168.75.128/itoken/itoken-config-respo.git
          search-paths: respo
          username: topsale@vip.qq.com
          password: 12345678
  zipkin:
    base-url: http://localhost:7020

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7000/eureka/

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info